import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gmail.michzuerch.CheckInBoard',
  appName: 'CheckInBoard',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
